spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blocksy
    username: blocksy
    password: blocksy123
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5s

minio:
  endpoint: http://localhost:9000
  access-key: admin
  secret-key: Admin@123456
  bucket-name: blocksy-media
  public-url: http://localhost:9000

logging:
  level:
    com.blocksy: debug
    org.springframework.security: info
